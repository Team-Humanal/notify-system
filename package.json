{
  "name": "notify-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "npm install",
    "start:gateway": "npm --workspace=@notify/gateway run dev",
    "build": "npm run build --workspaces",
    "lint": "echo \"no lint configured\""
  }
}
